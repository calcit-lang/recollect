# Upgrade Calcit toolchain to 0.13.29

- update `deps.cirru` and `@calcit/procs` through `caps upgrade --all`
- replace the unstable `Respo/respo-value.calcit@main` dependency with the stable 0.5.0 release
- migrate scripts, CI, and documentation from `cr` to `calcit`
- replace duplicated Respo/Calcit agent manuals with `calcit docs` lookups
