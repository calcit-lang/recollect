# Release 0.0.37

- Publish the atomic `Result`-returning validated patch API and typed diagnostic paths.
- Add the generic, method-oriented `PatchBatch.apply-to` boundary for realtime collaboration clients.
- Preserve the existing direct patch hot path while keeping native, JavaScript, WASM, and static quality gates green.
