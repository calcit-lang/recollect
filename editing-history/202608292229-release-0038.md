# Release 0.0.38

- Publish namespace-qualified validated patch schemas for downstream static inference.
- Preserve the 0.0.37 runtime API and behavior.
- Enable consumer-side `PatchBatch.apply-to` method specialization without dynamic fallback.
